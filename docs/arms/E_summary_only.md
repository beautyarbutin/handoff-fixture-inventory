T1 implemented the core inventory filtering logic in the catalog module and
added tests for category, price, and stock filtering. T2 should expose this
logic through the API search function and add API-level coverage while keeping
the existing catalog behavior intact.

